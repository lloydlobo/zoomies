export class Shape {
  x: number;

  y: number;

  velX: number;

  velY: number;
  constructor(x: number, y: number, velX: number, velY: number) {
    this.x = x;
    this.y = y;
    this.velX = velX;
    this.velY = velY;
  }
}
